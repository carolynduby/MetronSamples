DNS %{DNS_TIME:UNWANTED}%{DNS_THREAD_ID:UNWANTED}%{DNS_CONTEXT:UNWANTED}%{DNS_PACKET_ID:UNWANTED}%{DNS_PROTO:UNWANTED}%{DNS_SEND_REC:UNWANTED}%{DNS_SRC_IP:UNWANTED}%{BASE16NUM:xid} %{DNS_RESPONSE:reponse} %{DNS_OPCODE_FIELD:UNWANTED}%{DNS_RESPONSE_FLAGS:UNWANTED}%{DNS_QUESTION_TYPE:UNWANTED}%{GREEDYDATA:question_name}
DNS_AMPM (AM|PM)
DNS_TIME_FORMAT %{MONTHNUM:UNWANTED}[/-]%{MONTHDAY:UNWANTED}[/-]%{YEAR:UNWANTED} %{HOUR:UNWANTED}:%{MINUTE:UNWANTED}:%{SECOND:UNWANTED} %{DNS_AMPM:UNWANTED}
DNS_TIME %{DNS_TIME_FORMAT:timestamp}%{SPACE:UNWANTED}

DNS_THREAD_ID %{BASE16NUM:thread_id}%{SPACE:UNWANTED}

DNS_CONTEXT %{NOTSPACE:context}%{SPACE:UNWANTED}

DNS_PACKET_ID %{BASE16NUM:internal_packet_id}%{SPACE:UNWANTED}

DNS_PROTO %{WORD:proto}%{SPACE:UNWANTED}

DNS_SEND_REC %{WORD:send_rec}%{SPACE:UNWANTED}

DNS_SRC_IP %{IP:ip_src_addr}%{SPACE:UNWANTED}

DNS_RESPONSE ([\sR]{1})

DNS_OPCODE ([QNU\?]{1})
DNS_OPCODE_FIELD %{DNS_OPCODE:opcode}%{SPACE:UNWANTED}

DNS_CODE [A-Z ]
DNS_RESPONSE_FLAGS \[%{BASE16NUM:flags_hex}   %{DNS_CODE:flags_char}   %{WORD:reponse_code}\]%{SPACE:UNWANTED}

DNS_QUESTION_TYPE %{WORD:question_type}%{SPACE:UNWANTED}


